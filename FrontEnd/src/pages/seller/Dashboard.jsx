/**
 * dashboard.html in the prototype was the navbar and sidebar with nothing
 * between them, so this carries that across as-is rather than inventing a
 * dashboard the design has not decided on yet.
 */
export default function Dashboard() {
    return (
        <main className="sellerContent">
            <h1 className="sellerContentTitle">Dashboard</h1>

            <p className="sellerContentSubtitle">
                This feature is under development.
            </p>
        </main>
    );
}
